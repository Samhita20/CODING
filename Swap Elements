Write a C Program to read an array of 5 elements and swap the first and last, second and 4th and so on.

public static void swapElements(int[] a)
    {
        int n = 5;
        int i,j;
        for(i=0,j=n-1;i<n/2;i++,j--)
        {
            int t = a[i];
            a[i] = a[j];
            a[j] = t;
        }
        for(i=0;i<n;i++)
            System.out.println(a[i]+" ");
    }
